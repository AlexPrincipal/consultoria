'use client';
// This page is no longer used for editing.
// The layout will redirect any traffic from /admin to the homepage.

export default function AdminPage() {
    return (
        <div className="flex items-center justify-center min-h-[calc(100vh-7rem)]">
            <p>Redirigiendo a la página de inicio para la edición en línea...</p>
        </div>
    );
}
